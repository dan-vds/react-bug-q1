// Header Component
// Displays the main page title and description

export default function Header() {
    return (
        <header className="header">
            <h1>User Management Dashboard</h1>
            <p>Manage user accounts and view statistics</p>
        </header>
    );
  }